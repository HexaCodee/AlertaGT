# client-user/.npmrc
# React Native/Expo esperan una estructura node_modules "plana" (hoisted).
# Sin esto, pnpm usa symlinks que rompen la resolución de módulos nativos de Metro.
node-linker=hoisted
